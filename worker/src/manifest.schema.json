{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "PackageManifest",
  "description": "Metadata extracted from a built package.\n\nThe manifest merges two sources of truth:\n\n* declarative metadata from `.PKGINFO` (name, version, `depends=`, `provides=`, ...);\n* ABI facts extracted from the ELF objects shipped in the package\n  (`DT_SONAME` → [`provides`](Self::provides), `DT_NEEDED` + version needs →\n  [`requires`](Self::requires)).\n\nELF data refines the declarative dependencies; it never replaces them, because\nscripts, data files and `dlopen()`-loaded plugins are invisible to the ELF loader.",
  "type": "object",
  "properties": {
    "arch": {
      "type": "string"
    },
    "backup": {
      "description": "Paths that must be preserved on upgrade/removal (`backup=` in `.PKGINFO`).",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "conflicts": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/DependencyRule"
      }
    },
    "description": {
      "type": [
        "string",
        "null"
      ]
    },
    "filename": {
      "description": "Archive file name as produced by makepkg (`zlib-1:1.3.2-3-x86_64.pkg.tar.zst`).",
      "type": "string"
    },
    "files": {
      "description": "Absolute paths of every file installed by the package, used for collision\ndetection and `owns` queries. Directories end with `/`.",
      "type": "array",
      "default": [],
      "items": {
        "type": "string"
      }
    },
    "licenses": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "name": {
      "type": "string"
    },
    "optional": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/DependencyRule"
      }
    },
    "pkginfo": {
      "description": "Declarative metadata copied verbatim from `.PKGINFO`. This is what\n`repo-add` puts in a `desc` file; keep it untouched so generated pacman\ndatabases are indistinguishable from the real thing.",
      "$ref": "#/$defs/PkgInfoFields"
    },
    "provides": {
      "description": "Capabilities this package satisfies. Always includes `name=version`.\nExample: `libssl.so=3-64`, `openssl=3.3.1-1`, `web-browser`.",
      "type": "array",
      "default": [],
      "items": {
        "$ref": "#/$defs/DependencyRule"
      }
    },
    "replaces": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/DependencyRule"
      }
    },
    "requires": {
      "description": "Hard runtime requirements. Example: `libc.so.6`, `libc.so.6(GLIBC_2.38)`,\n`python>=3.12`.",
      "type": "array",
      "default": [],
      "items": {
        "$ref": "#/$defs/DependencyRule"
      }
    },
    "schema_version": {
      "description": "Schema version of this document. See [`MANIFEST_SCHEMA_VERSION`].",
      "type": "integer",
      "format": "uint32",
      "minimum": 0
    },
    "sha256": {
      "description": "Hex-encoded SHA-256 of the `.pkg.tar.zst` archive.",
      "type": "string"
    },
    "size_download": {
      "type": "integer",
      "format": "uint64",
      "minimum": 0
    },
    "size_installed": {
      "type": "integer",
      "format": "uint64",
      "minimum": 0
    },
    "url": {
      "type": [
        "string",
        "null"
      ]
    },
    "version": {
      "description": "Full version string in Arch format: `[epoch:]pkgver-pkgrel`.",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "required": [
    "schema_version",
    "name",
    "version",
    "arch",
    "size_installed",
    "size_download",
    "sha256",
    "filename",
    "pkginfo"
  ],
  "$defs": {
    "DependencyRule": {
      "description": "A single dependency or capability.\n\nThe optional `symbol_version` captures glibc-style symbol version needs\n(`libc.so.6(GLIBC_2.38)`) that come from the ELF `.gnu.version_r` section.",
      "type": "string"
    },
    "PkgInfoFields": {
      "description": "Verbatim `.PKGINFO` values, in Arch dependency syntax, without ELF enrichment.",
      "type": "object",
      "properties": {
        "base": {
          "description": "`pkgbase` — the PKGBUILD this package was built from.",
          "type": "string"
        },
        "builddate": {
          "description": "Unix timestamp from `builddate`.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "checkdepends": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "conflicts": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "depends": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "groups": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "makedepends": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "optdepends": {
          "description": "`name: description` entries, description preserved.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "packager": {
          "type": "string"
        },
        "provides": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "replaces": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false,
      "required": [
        "base",
        "builddate"
      ]
    }
  }
}
